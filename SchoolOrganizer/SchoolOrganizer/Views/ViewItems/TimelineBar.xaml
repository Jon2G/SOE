<?xml version="1.0" encoding="UTF-8"?>
<ContentView xmlns="http://xamarin.com/schemas/2014/forms" 
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:horario="clr-namespace:SchoolOrganizer.Models.Scheduler;assembly=SchoolOrganizer"
             xmlns:viewItems="clr-namespace:SchoolOrganizer.Views.ViewItems;assembly=SchoolOrganizer"
             xmlns:models="clr-namespace:SchoolOrganizer.ViewModels.ViewItems;assembly=SchoolOrganizer"
             x:Class="SchoolOrganizer.Views.ViewItems.TimelineBar"
             HorizontalOptions="FillAndExpand" x:Name="Me"
             VerticalOptions="FillAndExpand" BackgroundColor="White"
             WidthRequest="{x:Static models:TimelineBarViewModel.CollapsedWidth}">
    <ContentView.GestureRecognizers>
        <TapGestureRecognizer Tapped="TapGestureRecognizer_OnTapped"/>
        <SwipeGestureRecognizer Direction="Left" Command="{Binding ExpandCommand}" />
        <SwipeGestureRecognizer Direction="Right" Command="{Binding CollapseCommand}" />
    </ContentView.GestureRecognizers>
    <ContentView.BindingContext>
        <models:TimelineBarViewModel x:Name="Model"/>
    </ContentView.BindingContext>
    <ContentView.Content>
        <Grid>


            <ScrollView InputTransparent="True" VerticalScrollBarVisibility="Default" HorizontalScrollBarVisibility="Never"  Orientation="Vertical">
                <viewItems:SwipeContainer SwipeCommand="{Binding ToggleCommand}"
                                   CommandParameter="{x:Reference Me}">
                    <StackLayout Spacing="0"  BindableLayout.ItemsSource="{Binding Subjects}">
                        <BindableLayout.ItemTemplate>
                            <DataTemplate x:DataType="{x:Type horario:Subject}">
                                <Grid>
                                    <BoxView HeightRequest="120"  BackgroundColor="{Binding Color}"/>
                                    <StackLayout  Margin="0"  BackgroundColor="{Binding Color}"
                                                  IsVisible="{Binding IsExpanded, Source={x:Reference Model}}">
                                        <StackLayout Padding="5" >
                                            <Label TextColor="{DynamicResource secondaryTextColor}" Text="{Binding Name,Mode=OneWay}" FontFamily="KarlaSB" FontSize="18"/>
                                            <Label TextColor="{DynamicResource secondaryTextColor}" Text="{Binding Group}" FontFamily="KarlaSB" FontSize="18"/>
                                            <StackLayout Orientation="Horizontal"  >
                                                <Label TextColor="{DynamicResource secondaryTextColor}" Text="{Binding Begin,StringFormat='{0:hh}:{0:mm}'}" FontFamily="KarlaSB" FontSize="18"/>
                                                <Label TextColor="{DynamicResource secondaryTextColor}" Text="a" FontFamily="KarlaSB" FontSize="18"/>
                                                <Label TextColor="{DynamicResource secondaryTextColor}" Text="{Binding End,StringFormat='{0:hh}:{0:mm}'}" FontFamily="KarlaSB" FontSize="18"/>
                                            </StackLayout>
                                        </StackLayout>
                                    </StackLayout>
                                </Grid>
                            </DataTemplate>
                        </BindableLayout.ItemTemplate>
                    </StackLayout>
                </viewItems:SwipeContainer>

            </ScrollView>
        </Grid>
    </ContentView.Content>
</ContentView>